apiVersion: v1
kind: Service
metadata:
  name: {{ include "zerok-cli.fullname" . }}
  labels:
    {{- include "zerok-cli.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zerok-cli.selectorLabels" . | nindent 4 }}
