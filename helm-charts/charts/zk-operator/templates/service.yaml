apiVersion: v1
kind: Service
metadata:
  name: {{ include "zk-operator.fullname" . }}
  labels:
    {{- include "zk-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zk-operator.selectorLabels" . | nindent 4 }}
