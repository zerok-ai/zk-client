apiVersion: v1
kind: Service
metadata:
  name: {{ include "zk-scenario-manager.fullname" . }}
  labels:
    {{- include "zk-scenario-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zk-scenario-manager.selectorLabels" . | nindent 4 }}
