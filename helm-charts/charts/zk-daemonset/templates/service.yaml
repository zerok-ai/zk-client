apiVersion: v1
kind: Service
metadata:
  name: {{ include "zk-daemonset.fullname" . }}
  labels:
    {{- include "zk-daemonset.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zk-daemonset.selectorLabels" . | nindent 4 }}
